# Portfolio Context Intake

This directory is the queue for work delivered to the `portfolio-context`
workstream by another workstream or the project owner.

Add one item per file using
`YYYY-MM-DD-<sender-mnemonic>-<short-slug>.md`. Do not list queue items in the
root documentation index; the directory listing is authoritative.

# Project Management Intake

This directory is the queue for work that other workstreams or the project
owner deliver to `project-management` for routing or portfolio-level action.

Add one item per file using
`YYYY-MM-DD-<sender-mnemonic>-<short-slug>.md`. Do not list queue items in the
root documentation index; the directory listing is authoritative.
